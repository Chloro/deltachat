module.exports = angular.module('deltaChat.shared.components', [
  require('./alert-modal').name,
  require('./aria-live').name,
  require('./button-loading').name,
  require('./i').name,
  require('./keyboard-click').name,
  require('./nav-menu').name,
  require('./subheader').name,
  require('./tabs').name
]);
