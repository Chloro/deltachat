module.exports = angular.module('deltachat-ui.shared.components', [
  require('./a').name,
  require('./aria-live').name,
  require('./br').name,
  require('./form').name,
  require('./focus').name,
  require('./i').name,
  //require('./login').name,
  require('./modal').name,
  require('./nav-menu').name,
  require('./subheader').name
]);
